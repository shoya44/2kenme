# つぎどこ — フロントエンド設計書

- 文書ID: FE-001
- 版: 2.1
- 更新日: 2026-09-23
- 技術: Vite + React + TypeScript

## 1. 責務

F/Eは以下を担当する。

- 画面表示
- 条件入力
- 現在地取得
- `/api/search` 呼出
- ブロックシャッフル
- NG / OK処理
- 重複排除
- queue / セッション管理
- 条件緩和の段階管理
- 前回条件・履歴保存

HotPepper APIへ直接アクセスしない。HotPepperの予算コードを持たない。

## 2. 画面状態

```typescript
type Screen = "search" | "result";
```

```text
search
  ↓ さがす
result
  ├─ NG → result
  ├─ OK → HotPepper
  ├─ 候補なし → 緩和 → result
  └─ 条件変更 → search
```

React Routerは使用しない。

## 3. 共通ヘッダー

```text
[Menu]        つぎどこ        [ ]
```

- 左: SVGメニュー
- 中央: タイトル。**タップでトップ画面へ戻る**
- 右: レイアウト用スペーサー

タイトルは `<h1>` の中に `<button>` を置く。見出しとしての意味を保ったまま押せるようにするため。見た目は変えず、タップ領域は48px以上を確保する。

戻り先は「条件を変更」と同じで、条件は保持したままトップ画面へ遷移する。

## 4. トップ画面

```text
Header

予算                        4,000円以内
──────●────────

ジャンル
[おまかせ] [居酒屋] [ダイニングバー・バル] [バー・カクテル] …

こだわり
2件選択                              >

距離                                1km
──────●────────

[            さがす            ]

Powered by ホットペッパーグルメ Webサービス
```

UIは原則「ラベル / 現在値 / コントロール」で統一する。

## 5. 予算（上限指定）

```typescript
interface BudgetOption {
  /** 1人あたりの予算上限（円）。null は上限なし */
  max: number | null;
  label: string;
}

const BUDGET_OPTIONS: BudgetOption[] = [
  { max: 2000,  label: "2,000円以内" },
  { max: 3000,  label: "3,000円以内" },
  { max: 4000,  label: "4,000円以内" },
  { max: 5000,  label: "5,000円以内" },
  { max: 7000,  label: "7,000円以内" },
  { max: 10000, label: "10,000円以内" },
  { max: null,  label: "指定なし" },
];
```

UIはindexを操作し、APIには `budgetMax` を送る。HotPepperの予算コードへの変換はB/Eが行う。

初期値: `4000`（index 2）。

### 5.1 予算未登録の店も含める

スライダーの下にチェックボックス「予算未登録の店も含める」を置き、`includeUnknownBudget` を切り替える（REQ-001 §6.1）。

- 初期値: `true`
- `budgetMax === null` のときは `disabled`（絞り込みが発生しないため）
- 直下に補足文を常時表示する
  - ON: 「予算を登録していない店も候補に入ります。候補は増えますが、予算が合わないこともあります。」
  - OFF: 「予算を登録している店だけに絞ります。候補はかなり少なくなります。」

## 6. ジャンル

- 単一選択
- `おまかせ = null`
- HotPepperのジャンルマスタ（大ジャンル）に一致させる
- 選択中は差し色で塗り、`aria-pressed` を付与する
- 狭い画面では横スクロール

```typescript
interface GenreOption {
  code: string | null;
  label: string;
}

const GENRE_OPTIONS: GenreOption[] = [
  { code: null,   label: "おまかせ" },
  { code: "G001", label: "居酒屋" },
  { code: "G002", label: "ダイニングバー・バル" },
  { code: "G012", label: "バー・カクテル" },
  { code: "G013", label: "ラーメン" },
  { code: "G014", label: "カフェ・スイーツ" },
];
```

## 7. こだわり

初期状態は閉じる。

```text
こだわり
2件選択                            >
```

展開時:

- 個室
- 飲み放題
- 23時以降営業

深夜帯（`utils/latenight.ts` の判定、端末時刻で22:30〜翌5:00）は「23時以降営業」を既定でONにする（REQ-001 §6.3）。起動時に復元した条件へ `withLateNightDefault()` で適用する。

自動でONにしたことは黙って済ませず、アコーディオンの直下に一文出す（アコーディオンは閉じているため、中を開かないと気づけない）。

```text
深夜帯のため「23時以降営業」をONにしています。外すこともできます。
```

ユーザーが外せばその条件で検索し、「さがす」で保存される。次回も深夜帯ならまたONになる（「深夜は開いている店から出す」を既定とする判断）。

## 8. 距離

| index | 表示 | range |
|---|---|---|
| 0 | 300m | 1 |
| 1 | 500m | 2 |
| 2 | 1km | 3 |
| 3 | 2km | 4 |

初期値: `3`（1km）。

## 9. スライダー実装

予算・距離の離散スライダーは `<input type="range">` をベースに実装する。

- `min=0 / max=options.length - 1 / step=1`
- 目盛りラベルはあくまで装飾とし、`aria-hidden="true"`
- `aria-valuetext` に表示ラベル（例: `4,000円以内`）を設定
- つまみのタップ領域は48px以上を確保
- フォーカスリングを消さない

カスタムdiv実装は行わない（キーボード操作とスクリーンリーダー対応のコストが見合わないため）。

## 10. さがすボタン

状態:

```text
通常: さがす
位置情報取得中: 現在地を取得中…
API通信中: 検索中…
```

通信中は多重押下防止。

## 11. 結果画面

```text
Header

現在地から半径1km以内
ジャンル指定を外して再検索しました        ← 緩和時のみ

[ 店舗写真 ]

炭火焼き鳥 とり吉
↗ hotpepper.jp/...

徒歩 約4分
3,001〜4,000円
営業時間  月～日: 17:00～翌2:00
定休日    日曜日
連絡先    店舗詳細で確認
みんなに共有                              ← Web Share API が使える端末のみ

[ X  NG ]     [ ✓  OK ]

条件を変更

Powered by ホットペッパーグルメ Webサービス
```

結果項目:

- 検索範囲テキスト
- 緩和通知（該当時）
- 店舗写真
- 店名
- HotPepper URL
- 徒歩時間（`walkMinutes` が null なら行ごと出さない）
- 予算感（`budgetText` が null なら行ごと出さない）
- 営業時間 / 定休日（`openText` / `closedText` が null なら行ごと出さない）
- 連絡先導線
- 共有導線（`navigator.share` が使える端末のみ。`services/share.ts`）
- NG / OK

評価・レビュー・大量のタグ・地図は表示しない。

店舗写真は画面の主役なので遅延読み込み（`loading="lazy"`）しない。次候補（`queue[0]`）の写真は `App.tsx` で先読みし、NG直後に写真が遅れて出ないようにする。

「みんなに共有」は店名と HotPepper URL を端末の共有シートへ渡す。2軒目は一緒にいる人と決めるため、LINE等へそのまま送れるようにする。共有シートを閉じた場合（`AbortError`）は何もしない。使えない環境では行ごと出さず、クリップボードへの代替もしない。

営業時間・定休日はB/Eから来た自由記述をそのまま出す。「営業中」「もう閉まっています」といった判定表示はしない（BE-001 §11）。

## 12. NG

1. `currentShop.id` を `shownIds` へ追加
2. queueから次店舗を取り出す
3. 結果を更新
4. セッション保存
5. queue残数が8件以下、かつ `hasMore` なら追加検索
6. queueが空、かつ `hasMore === false` なら `noCandidate` 状態へ

## 13. OK

OKボタンは `<a>` 要素として実装する。

```tsx
<a
  href={currentShop.hotpepperUrl}
  target="_blank"
  rel="noopener noreferrer"
  onClick={handleOk}
>
  OK
</a>
```

`handleOk` は**同期処理のみ**で以下を行う。

1. 現在店舗を履歴へ保存
2. セッション更新

`window.open` を使ったプログラム遷移は行わない。非同期処理の後に呼ぶとiOS Safariでブロックされるため。

電話番号・メニュー・予約はHotPepper店舗詳細で確認する。

## 14. 候補なし

`noCandidate` は以下の2ケースで共通とする。

- 検索結果が0件で、続きもない（`hasMore === false`）
- 候補を出し切り、追加取得もできない

**そのページの候補が0件でも `hasMore` が真なら `noCandidate` にしない。** B/Eの予算絞り込みや重複排除はページ単位で全滅し得るため、そこで打ち切ると候補が残っているのに「候補なし」を出してしまう。スケルトンを出したまま次ページの先読みへ回す（§19・§20と同じ経路）。

```text
候補が見つかりませんでした

[ こだわり条件を外してさがす ]     ← relaxLevel に応じて文言が変わる

条件を変更
```

`relaxLevel` が4（全緩和済み）の場合は緩和CTAを出さず、「条件を変更」のみを表示する。

## 15. 条件緩和

```typescript
type RelaxLevel = 0 | 1 | 2 | 3 | 4;
```

| relaxLevel | 適用内容 | CTA文言 | 結果画面の通知 |
|---|---|---|---|
| 1 | preferences を全false | こだわり条件を外してさがす | こだわり条件を外して再検索しました |
| 2 | genreCode を null | ジャンルをおまかせにしてさがす | ジャンル指定を外して再検索しました |
| 3 | range を1段階拡大 | もう少し広い範囲でさがす | 範囲を広げて再検索しました |
| 4 | budgetMax を null | 予算の上限を外してさがす | 予算の上限を外して再検索しました |

- 緩和は累積する（relaxLevel=3 なら 1・2 の緩和も適用済み）
- **現在の条件を実際に変えない段階はスキップする**。`nextRelaxLevel(condition, level)` が、`level` より後ろで初めて条件が変わる段階を返す（変わる段階がなければ `null` ＝ CTAを出さない）
  - level 1: `preferences` がすべて false ならスキップ
  - level 2: `genreCode` が null ならスキップ
  - level 3: `range` が既に4（2km）ならスキップ
  - level 4: `budgetMax` が null ならスキップ
  - これがないと、こだわり未設定のまま「こだわり条件を外してさがす」を押すと**まったく同じ条件で再検索**され、条件は変わっていないのに緩和したように見える混乱が起きる
- 緩和・再試行での再検索は `shownIds` を引き継ぐ（同じ抽選の続きとして扱い、NGした店を再提示しない）。トップの「さがす」からの検索では捨てる
- 緩和は**ユーザーが明示的にCTAを押した場合のみ**適用する。自動緩和はしない
- 緩和した条件は `condition` 本体を書き換えず、`utils/relax.ts` の `applyRelax(condition, level)` で導出する（「条件を変更」でトップへ戻ったとき元の条件が見えるようにするため）

```typescript
function applyRelax(
  condition: SearchCondition,
  level: RelaxLevel
): SearchCondition;
```

## 16. AppState

```typescript
type ErrorKind = "location" | "network";

interface AppState {
  screen: "search" | "result";
  condition: SearchCondition;
  relaxLevel: RelaxLevel;
  location: GeoPoint | null;
  currentShop: Shop | null;
  queue: Shop[];
  shownIds: string[];
  nextStart: number;
  hasMore: boolean;
  /** 検索中。「さがす」の多重押下を防ぐ */
  loading: boolean;
  /** 現在地の取得中 */
  locating: boolean;
  /** 先読み中。同時に1つまで */
  prefetching: boolean;
  /** 先読みが失敗して止まっている。次のNGまで再開しない */
  prefetchPaused: boolean;
  noCandidate: boolean;
  error: ErrorKind | null;
  /** 検索開始時刻。古い応答を捨てる世代と、セッションTTLの基準 */
  startedAt: number | null;
}
```

`useReducer` を基本とする（`state/reducer.ts`）。非同期処理の応答は `startedAt` で世代を比べ、新しい検索が始まっていれば捨てる。

## 17. SearchCondition

```typescript
interface SearchCondition {
  /** 1人あたりの予算上限（円）。null は上限なし */
  budgetMax: number | null;
  /** 予算が未登録の店も候補に含めるか（REQ-001 §6.1） */
  includeUnknownBudget: boolean;
  genreCode: string | null;
  preferences: {
    privateRoom: boolean;
    freeDrink: boolean;
    midnight: boolean;
  };
  range: 1 | 2 | 3 | 4;
}
```

## 18. API通信

```typescript
async function fetchShops(
  condition: SearchCondition,
  location: GeoPoint,
  start: number,
  passcode: string | null
): Promise<SearchResponse>
```

合言葉は `X-App-Token` ヘッダで送る（ヘッダ名は `shared/api-types.ts` の `APP_TOKEN_HEADER`）。失敗は `SearchApiError`（HTTPステータス付き）で投げ、`403` だけは合言葉の拒否として扱う（§29）。

```text
POST /api/search
```

API I/F型は `shared/api-types.ts` を共有する。

## 19. 提示ロジック

```typescript
shuffleInChunks(shops: Shop[], chunkSize: number): Shop[]
mergeUniqueShops(state, incoming): Shop[]
shouldPrefetch(state): boolean
```

```text
初回: 50件（おすすめ順） → 表示済み/重複を除外 → 10件ブロック単位でシャッフル → queue → 1件表示
NG: current → shownIds → 次候補
prefetch: queue.length <= 8 && hasMore && !prefetching && !prefetchPaused
このページが0件 & hasMore → スケルトンのまま次ページへ（上限はB/Eの MAX_START）
prefetch失敗: prefetchPaused = true（次のNGで解除）
```

**先読みに失敗したら、次のNGまで自動では再試行しない。** 失敗で `prefetching` が戻った直後に再び条件を満たすため、これが無いと通信が切れている間ずっと上流を叩き続ける（失敗 → 即再試行 のループ）。NGはユーザー操作なので、そこで改めて試みる。手元に候補が無い状態で失敗したときだけエラーと再試行CTAを出す（BAS-001 §14）。先読み中の現在地取得失敗も同じ扱いで、表示中の店を消さない。

`shuffleInChunks` は配列を先頭から `chunkSize` 件ずつに区切り、**各ブロック内でのみ** Fisher-Yatesシャッフルを行う。これにより、HotPepperのおすすめ順（＝評判の良さと近さを反映した順序）を大枠で維持しつつ、毎回まったく同じ順序にならないようにする。

同時prefetchは1つまで（`prefetching` フラグで制御）。

## 20. 先読み中の候補切れ

prefetch中にNG連打でqueueが空になった場合:

- 結果カードをスケルトン表示に切り替える
- NG / OKボタンを無効化する
- prefetch完了後に次候補を表示する
- prefetchが0件で終わっても `hasMore` が真なら、続けて次ページを先読みする
- prefetchが0件で終わり `hasMore === false` になったら `noCandidate` へ
- prefetchが失敗し、手元に候補が無ければ通信エラー（再試行CTA）へ。候補があれば何も出さず、次のNGで再試行する
- prefetchが失敗し、手元に候補が無ければ通信エラー（再試行CTA）へ。候補があれば何も出さず、次のNGで再試行する

## 21. Geolocation

- **初回は「さがす」押下時**に取得を開始する。許可ダイアログが出るため、押下と同じ操作文脈で要求する
- **一度許可が済んでいれば、次回以降は起動時に先出しする**（`canPrimeLocation()`）。検索の待ち時間が縮む。ユーザー操作を伴わないので、先出しの失敗は画面に出さず「さがす」で取り直す
- 取得成功後はsessionStorageへ保持し、鮮度（`GEO_TTL_MS` = 5分）内なら再取得しない
- **鮮度切れの現在地は使わない**（`isGeoFresh()`）。`state.location` にも同じ判定をかける。1軒目から歩いたあとの再検索で古い地点を使わないため
- 失敗時は再試行CTAを表示する

先出しの可否判定:

| 許可実績（localStorage） | Permissions API | 先出し |
|---|---|---|
| なし | — | しない（初回はダイアログを出さない） |
| あり | `granted` | する |
| あり | `prompt` / `denied` | しない（あとから取り消された） |
| あり | 照会できない（Safari 等） | する |

現在地（座標）はsessionStorage以外へ保存しない。

## 22. 検索範囲の表示

検索範囲を以下の2箇所に表示する。

| 画面 | 位置 | 文言 |
|---|---|---|
| トップ | 「さがす」ボタンの直上 | 現在地から半径1km以内でさがします |
| 結果 / 候補なし | ヘッダー直下 | 現在地から半径1km以内 |

- 地図・座標は表示しない
- `range` の表示ラベル（300m / 500m / 1km / 2km）をそのまま使う
- 緩和で `range` が変わった場合は緩和後の値を表示する
- トップ画面での表示は、検索前に「どこを中心に探すのか」を示して不安を減らすことを目的とする

### 22.1 検索条件の表示

結果画面と候補なし画面に、**実際に検索に使った条件**（＝緩和を適用したあとの条件）を1行で表示する。緩和CTAを押したときに何が変わったのかを、ユーザーが自分で確認できるようにするため。

- 生成: `describeCondition(condition)` — `予算 / ジャンル / 距離 / こだわり` を ` / ` で連結する
  - 例: `4,000円以内 / おまかせ / 1km以内 / こだわりなし`
  - 予算未指定は `予算 指定なし`、こだわり未設定は `こだわりなし`、こだわり複数は `・` で連結
- 位置: 結果画面は緩和通知の下、候補なし画面は見出しの下
- 語の途中で折り返さないよう `word-break: keep-all` を指定する（区切りの空白でのみ折る）
- `App.tsx` で `applyRelax(condition, relaxLevel)` を一度だけ計算し、両画面に渡す

## 23. Storage

```text
localStorage
├── tsugidoko:defaults
├── tsugidoko:history
├── tsugidoko:session      （離脱から5分 / 開始から2時間）
├── tsugidoko:geo-granted  （位置情報の許可実績。座標は含まない）
└── tsugidoko:passcode

sessionStorage
└── tsugidoko:geo
```

`tsugidoko:defaults` は読込時に予算・ジャンル・距離を許可値と照合し、外れていれば初期値へ戻す（旧バージョンが書いた値で Worker が 400 を返し続けるのを防ぐ。DATA-001 §4）。

UIから直接Web Storage APIを呼ばず `services/storage.ts` へ集約する。

起動時に `tsugidoko:session` が有効期間内（DATA-001 §7）であれば、結果画面から再開する。**離脱から5分を超えていればトップ画面から始める**（前回のNG履歴もセッションごと捨てる）。ただし現在地はsessionStorageにしか無いため、再開後の追加取得時に現在地が無ければ再取得する。

画面が `hidden` になったら `touchSession()` で離脱時刻を控える。

## 24. コンポーネント

```text
src/
├── components/
│   ├── Header.tsx              # 共通ヘッダー（§3）
│   ├── DiscreteSlider.tsx      # 予算・距離の離散スライダー（§9）
│   ├── BudgetSection.tsx       # 予算スライダー + 未登録トグル（§5）
│   ├── GenreChips.tsx          # §6
│   ├── PreferenceAccordion.tsx # §7
│   ├── ShopResult.tsx          # 結果カード・NG / OK・共有（§11〜§13）
│   ├── ResultSkeleton.tsx      # §20
│   ├── NoCandidate.tsx         # §14
│   ├── ErrorNotice.tsx         # §28
│   ├── HotpepperCredit.tsx     # §26
│   ├── Sheet.tsx               # 下から出るシートの土台
│   ├── MenuSheet.tsx
│   ├── HistorySheet.tsx
│   ├── AboutSheet.tsx
│   ├── icons.tsx               # SVGアイコン（§27）
│   └── ui.module.css           # 画面共通のスタイル
├── screens/
│   ├── PasscodeScreen.tsx      # §29
│   ├── SearchScreen.tsx        # §4
│   └── ResultScreen.tsx        # §11
├── services/
│   ├── api.ts                  # §18
│   ├── storage.ts              # §23
│   ├── location.ts             # §21
│   └── share.ts                # §11 共有
├── state/
│   └── reducer.ts              # §16, §19
├── utils/
│   ├── shuffle.ts
│   ├── relax.ts                # §15
│   └── latenight.ts            # §7
├── constants.ts                # 選択肢・表示ラベル・describeCondition
├── types.ts
├── tokens.css                  # §25
├── index.css                   # リセットとタップ体感（§33）
├── App.tsx
└── main.tsx
```

`PartySizeStepper.tsx` はMVPでは作成しない。

## 25. デザイン

```css
--bg: #111214;
--surface: #191A1D;
--surface-subtle: rgba(255,255,255,.04);
--text-primary: #F5F5F5;
--text-secondary: rgba(255,255,255,.58);
--text-tertiary: rgba(255,255,255,.38);
--border: rgba(255,255,255,.08);
--accent: #D7C2A0;
--on-accent: #111214;

--safe-top: env(safe-area-inset-top, 0px);
--safe-bottom: env(safe-area-inset-bottom, 0px);
```

- 左右余白: 24px
- セクション間: 24〜32px
- ボタン高: 48px以上
- ボタン角丸: 16px
- 写真角丸: 20px
- Animation: 150〜250ms
- Glass表現: 弱く限定

### セーフエリア

`env()` を直接使わず変数を経由する。ブラウザで値を差し替えてノッチ端末を再現・検証できるようにするため。

**高さを固定した要素に `padding-top: var(--safe-top)` を足さない。** `box-sizing: border-box` のもとでは padding が高さを食い潰し、ノッチ端末のPWAで中身が潰れる。`min-height: calc(本来の高さ + var(--safe-top))` を使う。

ヘッダーで実際にこの事故を起こした。`height: 58px` に約59pxの `padding-top` が入り、中身の高さが0になって本文と重なった。Safariのタブでは inset が0のため再現せず、PWAでのみ壊れる。

## 26. HotPepperクレジット

```tsx
<small className={styles.credit}>
  Powered by ホットペッパーグルメ Webサービス
</small>
```

- 表示箇所: トップ画面下部 / 結果画面下部 / 履歴シート下部
- 色は `--text-tertiary`、フォントサイズ11px程度で小さく薄く配置する
- `HotpepperCredit.tsx` として共通化する

## 27. アクセシビリティ

- SVGへ `aria-label`（装飾アイコンは `aria-hidden`）
- ジャンルチップは `aria-pressed` で選択状態を伝える（視覚表現は差し色のみでよい）
- スライダーは `<input type="range">` + `aria-valuetext`
- Focusを削除しない
- テキストコントラストを確保する
- `prefers-reduced-motion` 対応

## 28. エラー

```text
位置情報: 現在地を取得できませんでした [再試行]
候補なし: 候補が見つかりませんでした [条件をゆるめるCTA]
通信:     店舗を取得できませんでした [再試行]
          （navigator.onLine が false なら「オフラインのようです。電波のある場所でもう一度おためしください。」を添える）
```

## 29. 合言葉

合言葉（`tsugidoko:passcode`）が未保存なら、条件設定の代わりに入力画面を出す。

```text
合言葉
[ ************ ]

このアプリは身内で使うため、合言葉が必要です。
一度入れればこの端末では次回から聞きません。

[ はじめる ]
```

- 入力は `type="password"`。空のままでは進めない（ボタンを無効化）
- 保存後はそのまま条件設定へ進む。次回起動時は聞かない
- `/api/search` が `403` を返したら保存値を破棄し、「合言葉が違います」を添えてこの画面へ戻す
- ヘッダー（メニュー・このアプリについて）はこの画面でも使える。HotPepperのデータを出さない画面なのでクレジットは置かない
- 検索の合言葉は `X-App-Token` ヘッダで送る（BE-001 §5）

## 30. PWA

`vite-plugin-pwa` を使用。

- アプリシェル: キャッシュ
- APIレスポンス: 永続キャッシュしない
- 店舗写真: 必須キャッシュにしない
- `manifest.name`: つぎどこ
- `manifest.short_name`: つぎどこ

## 31. モック

`docs/mock/` に版2.0のモックを置く。本書の内容に一致させてある。

| 画像 | 画面 | 本書 |
|---|---|---|
| `docs/mock/01_トップ画面.png` | トップ | §4 |
| `docs/mock/02_結果画面.png` | 結果 | §11 |
| `docs/mock/03_候補なし.png` | 候補なし | §14 |
| `docs/mock/04_履歴シート.png` | 履歴シート | §24 |

`docs/mock/mock.html` が生成元で、`node docs/mock/shot.mjs` で再生成する。仕様を変えたらモックも同じPRで更新する。

モックは寸法・余白の目安であり確定値ではない。デザイントークンと数値の正は§25。

結果画面の写真は版1.0のモックから流用したプレースホルダーで、実在店舗の写真ではない。

版1.0（「運命の1軒」）のモックは `docs/mock/old/` に残してある。現行仕様ではないので参照時は注意する。差分は `docs/mock/old/README.md` を参照。

## 32. F/Eテスト観点

FE-001に対応するテストはTEST-001（06_テスト・CI設計書）を参照。

## 33. スマートフォンでの体感

iPhone（Safari / ホーム画面PWA）を主な利用環境とし、見た目を変えずに操作の体感を上げる。

| 項目 | 実装 | 目的 |
|---|---|---|
| 画面切替でスクロールを先頭へ | `App.tsx` で `screen` が変わるたびに `window.scrollTo(0, 0)` | トップ画面下端の「さがす」を押した位置が結果画面に持ち越され、カードが途中から見えるのを防ぐ |
| 次候補の写真を先読み | `queue[0].photoUrl` を `new Image()` で読む | NG直後に写真が遅れて出ない |
| タップ遅延の除去 | `button / a / label / summary / input[type=range]` に `touch-action: manipulation` | ダブルタップ判定待ちの遅延を無くす |
| タップ時の灰色フラッシュ・長押しメニューの抑制 | `-webkit-tap-highlight-color: transparent` / `-webkit-touch-callout: none` / `user-select: none`（ボタン） | NG連打で選択メニューやフラッシュが出ない |
| シート裏へのスクロール連動を止める | `.sheet { overscroll-behavior: contain }` | シートを最後までスクロールしても本文が動かない |
| 合言葉の入力 | `enterKeyHint="go"`、フォントサイズ16px | キーボードに「開く」を出す。フォーカス時に画面が拡大しない |
| 共有 | §11 | 一緒にいる人へ店をそのまま送れる |

`index.css` に置く。個別コンポーネントには書かない。

## 33. スマートフォンでの体感

iPhone（Safari / ホーム画面PWA）を主な利用環境とし、見た目を変えずに操作の体感を上げる。

| 項目 | 実装 | 目的 |
|---|---|---|
| 画面切替でスクロールを先頭へ | `App.tsx` で `screen` が変わるたびに `window.scrollTo(0, 0)` | トップ画面下端の「さがす」を押した位置が結果画面に持ち越され、カードが途中から見えるのを防ぐ |
| 次候補の写真を先読み | `queue[0].photoUrl` を `new Image()` で読む | NG直後に写真が遅れて出ない |
| タップ遅延の除去 | `button / a / label / summary / input[type=range]` に `touch-action: manipulation` | ダブルタップ判定待ちの遅延を無くす |
| タップ時の灰色フラッシュ・長押しメニューの抑制 | `-webkit-tap-highlight-color: transparent` / `-webkit-touch-callout: none` / `user-select: none`（ボタン） | NG連打で選択メニューやフラッシュが出ない |
| シート裏へのスクロール連動を止める | `.sheet { overscroll-behavior: contain }` | シートを最後までスクロールしても本文が動かない |
| 合言葉の入力 | `enterKeyHint="go"`、フォントサイズ16px | キーボードに「開く」を出す。フォーカス時に画面が拡大しない |
| 共有 | §11 | 一緒にいる人へ店をそのまま送れる |

`index.css` に置く。個別コンポーネントには書かない。
